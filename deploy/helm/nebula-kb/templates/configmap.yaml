apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  NEBULA_DB_HOST: {{ .Release.Name }}-postgresql
  NEBULA_REDIS_HOST: {{ .Release.Name }}-redis-master
